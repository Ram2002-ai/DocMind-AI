# Base URL of your DocuMind AI backend (FastAPI/Flask/Express/etc.)
# No trailing slash.
VITE_API_BASE_URL=http://localhost:8000

# Set to "true" to run the UI fully against the built-in mock backend
# (no server required) — useful for frontend-only development.
VITE_USE_MOCK_API=true
